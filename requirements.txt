# Smart Shygyn PRO v3 — requirements.txt
# Обновлён согласно ТЗ (Разделы 1, 2, 4, 5)

# ── Уже есть (не менять) ──────────────────────────────────────────────
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
wntr>=1.2.0
plotly>=5.15.0
matplotlib>=3.7.0
networkx>=3.1
scipy>=1.11.0
folium>=0.14.0
streamlit-folium>=0.15.0
requests>=2.31.0
openpyxl>=3.1.0
xlrd>=2.0.1
gdown>=5.1.0

# ── Раздел 1: ML Engine ───────────────────────────────────────────────
scikit-learn>=1.3.0        # Isolation Forest
joblib>=1.3.0              # Сохранение ML моделей

# ── Раздел 4: PDF экспорт ─────────────────────────────────────────────
reportlab>=4.0.0           # Генерация PDF отчётов

# ── Раздел 5: FastAPI ─────────────────────────────────────────────────
fastapi>=0.104.0           # REST API endpoint
uvicorn>=0.24.0            # ASGI сервер для FastAPI
pydantic>=2.0.0            # Валидация данных в API

# ── Опционально: LSTM (Раздел 1.2, не обязательно для конкурса) ──────
# tensorflow>=2.13.0       # LSTM автоэнкодер (тяжёлая зависимость)
# torch>=2.0.0             # Альтернатива TensorFlow
